name: PipelineWorkflow

on:
  push:
    branches:
      - main
  pull_request:
    branches: 
      - main

jobs:
    build:
        name: Azure Pipeline
        runs-on: ubuntu-latest
        steps:
        - name: Azure Pipelines Action
          uses: Azure/pipelines@v1
          with:
            azure-devops-project-url: https://dev.azure.com/minotauroolimpo0464/DevProject
            azure-pipeline-name: 'WebProjectTest'
            azure-devops-token: ${{ AZURE_DEVOPS_TOKEN }}